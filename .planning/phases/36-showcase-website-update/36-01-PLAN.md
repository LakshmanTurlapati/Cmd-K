---
phase: 36-showcase-website-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/js/main.js
  - showcase/index.html
  - showcase/css/home.css
autonomous: true
requirements:
  - WEB-01-VERSION
  - WEB-02-DOWNLOADS
  - WEB-03-CONTENT

must_haves:
  truths:
    - "All version strings on index.html show v0.3.9 (footer, demo settings, download URLs)"
    - "Visitor on macOS sees macOS download button highlighted as primary"
    - "Visitor on Linux sees Linux download button highlighted as primary"
    - "Clicking Linux download shows a lightweight popup with x86_64 and aarch64 choices"
    - "All three platforms (macOS, Windows, Linux) are visible as download options"
    - "Feature cards mention Linux alongside macOS and Windows"
    - "GNOME Terminal appears in the terminal carousel"
    - "A Linux platform badge appears in the hero section"
    - "CTA section download button uses the data-download pattern for OS-aware URL"
  artifacts:
    - path: "showcase/js/main.js"
      provides: "VERSION constant, URLS map, OS detection, data-attribute population, arch popup toggle"
      contains: "VERSION"
    - path: "showcase/index.html"
      provides: "Download buttons with data-download attrs, Linux badge, updated feature cards, GNOME Terminal in carousel"
      contains: "data-download"
    - path: "showcase/css/home.css"
      provides: "Arch popup styles, platform-detected highlight, download-secondary row"
      contains: "arch-popup"
  key_links:
    - from: "showcase/js/main.js"
      to: "showcase/index.html"
      via: "data-download and data-version attributes"
      pattern: "querySelectorAll.*data-download"
    - from: "showcase/js/main.js"
      to: "showcase/index.html"
      via: "linux-arch-popup toggle"
      pattern: "linux-arch-popup"
---

<objective>
Update the showcase index page with v0.3.9 version infrastructure, platform-specific download buttons with OS auto-detection, Linux arch chooser popup, feature card text updates, GNOME Terminal carousel entry, and Linux platform badge.

Purpose: The showcase website needs to reflect that CMD+K now supports Linux alongside macOS and Windows, with proper download links for all platforms.
Output: Updated index.html with Linux downloads, main.js with version/OS logic, home.css with popup and layout styles.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-showcase-website-update/36-CONTEXT.md
@.planning/phases/36-showcase-website-update/36-RESEARCH.md
@showcase/js/main.js
@showcase/index.html
@showcase/css/home.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Version infrastructure and OS detection in main.js</name>
  <files>showcase/js/main.js</files>
  <action>
Add version infrastructure and OS detection to main.js, inside the existing IIFE before the DOMContentLoaded handler:

1. Add at the top of the IIFE (after `'use strict';`):
```javascript
var VERSION = '0.3.9';
var DOWNLOAD_BASE = 'https://github.com/LakshmanTurlapati/Cmd-K/releases/download';
var URLS = {
  macos: DOWNLOAD_BASE + '/v' + VERSION + '/CMD+K-' + VERSION + '-universal.dmg',
  windows: DOWNLOAD_BASE + '/v' + VERSION + '/CMD+K-' + VERSION + '-windows-x64.exe',
  linux_x86: DOWNLOAD_BASE + '/v' + VERSION + '/CMD+K-' + VERSION + '-linux-x86_64.AppImage',
  linux_arm: DOWNLOAD_BASE + '/v' + VERSION + '/CMD+K-' + VERSION + '-linux-aarch64.AppImage'
};

function detectOS() {
  var ua = navigator.userAgent;
  if (/Linux/.test(ua) && !/Android/.test(ua)) return 'linux';
  if (/Mac/.test(ua)) return 'macos';
  if (/Win/.test(ua)) return 'windows';
  return null;
}
```

2. Inside the existing DOMContentLoaded handler, add at the END (after the active nav link section):

a) **Version and download URL population:**
```javascript
// ---- Version & download URLs ----
document.querySelectorAll('[data-download]').forEach(function(el) {
  var key = el.getAttribute('data-download');
  if (URLS[key]) el.href = URLS[key];
});
document.querySelectorAll('[data-version]').forEach(function(el) {
  el.textContent = 'v' + VERSION;
});
```

b) **OS detection and primary button highlight:**
```javascript
// ---- OS auto-detect ----
var detectedOS = detectOS();
if (detectedOS) {
  document.querySelectorAll('[data-platform="' + detectedOS + '"]').forEach(function(btn) {
    btn.classList.add('platform-detected');
  });
}
```

c) **Linux arch popup toggle:**
```javascript
// ---- Linux arch popup ----
var linuxBtns = document.querySelectorAll('.linux-download-btn');
linuxBtns.forEach(function(btn) {
  var popup = btn.parentElement.querySelector('.arch-popup');
  if (!popup) return;
  btn.addEventListener('click', function(e) {
    e.preventDefault();
    popup.classList.toggle('open');
  });
});

// Close arch popup on click outside
document.addEventListener('click', function(e) {
  if (!e.target.closest('.linux-download-wrapper')) {
    document.querySelectorAll('.arch-popup.open').forEach(function(p) {
      p.classList.remove('open');
    });
  }
});
```

Keep ALL existing main.js code intact. Only add new code.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'); var c=fs.readFileSync('showcase/js/main.js','utf8'); var checks=['VERSION','DOWNLOAD_BASE','detectOS','data-download','data-version','data-platform','arch-popup','linux-download-btn']; var missing=checks.filter(function(s){return c.indexOf(s)===-1}); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('All patterns found in main.js')"</automated>
  </verify>
  <done>main.js contains VERSION constant, URLS map, detectOS function, data-attribute population, OS highlight logic, and arch popup toggle. All existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Update index.html with Linux downloads, badges, cards, and carousel</name>
  <files>showcase/index.html, showcase/css/home.css</files>
  <action>
**index.html changes:**

1. **Hero download buttons (lines 65-78):** Replace the entire `.hero-buttons` div with a two-tier layout:
   - Primary row: Three download buttons (macOS, Windows, Linux) + View Source, all using `data-download` attributes instead of hardcoded URLs. The Linux button is wrapped in a `.linux-download-wrapper` (position:relative) containing the button and an `.arch-popup` div.
   - Each download button gets a `data-platform` attribute matching its OS for the auto-detect highlight.
   - macOS button: `data-download="macos"` `data-platform="macos"` with Apple SVG icon
   - Windows button: `data-download="windows"` `data-platform="windows"` with Windows SVG icon
   - Linux button: class `linux-download-btn` `data-platform="linux"` with Tux SVG icon (use simplified Tux path from RESEARCH.md). No `href` -- clicking opens the arch popup.
   - Arch popup HTML (inside `.linux-download-wrapper`):
     ```html
     <div class="arch-popup" id="hero-arch-popup">
       <a data-download="linux_x86" class="arch-option" target="_blank" rel="noopener">
         <strong>x86_64</strong>
         <span>Intel / AMD (most common)</span>
       </a>
       <a data-download="linux_arm" class="arch-option" target="_blank" rel="noopener">
         <strong>aarch64</strong>
         <span>ARM (Raspberry Pi, etc.)</span>
       </a>
     </div>
     ```
   - Keep the View Source button unchanged.

2. **Add Linux platform badge** after the hero-buttons div, before the hero-terminal div. A small row of platform badges:
   ```html
   <div class="platform-badges reveal reveal-delay-3">
     <span class="platform-badge" title="macOS">
       <svg width="14" height="14" viewBox="0 0 814 1000" fill="currentColor"><!-- Apple icon (same SVG as download button) --></svg>
       macOS
     </span>
     <span class="platform-badge" title="Windows">
       <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><!-- Windows icon --></svg>
       Windows
     </span>
     <span class="platform-badge" title="Linux">
       <svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M12.504 0c-.155 0-.315.008-.48.021-4.226.333-3.105 4.807-3.17 6.298-.076 1.092-.323 1.396-1.743 2.817-1.659 1.717-3.203 4.093-3.203 5.795 0 2.153 1.222 3.705 2.996 4.627-.097.205-.158.413-.158.652 0 .921.738 1.79 2.243 1.79 1.262 0 2.088-.679 2.506-1.309.42.63 1.244 1.309 2.506 1.309 1.505 0 2.243-.869 2.243-1.79 0-.239-.061-.447-.158-.652C17.78 18.774 19 17.222 19 15.069c0-1.702-1.544-4.078-3.203-5.795-1.42-1.421-1.667-1.725-1.743-2.817-.065-1.491 1.056-5.965-3.17-6.298-.165-.013-.325-.021-.48-.021z"/></svg>
       Linux
     </span>
   </div>
   ```

3. **Feature cards (lines 127-174):**
   - "System-Wide Overlay" (line 131-133): Change the `<p>` to: "Always one keystroke away on macOS, Windows, and Linux. A minimal command bar that appears when you need it and vanishes when you don't."
   - "Multi-Terminal Support" (line 155-157): Change the `<p>` to: "Compatible with all major terminals on macOS, Windows, and Linux -- including GNOME Terminal, kitty, WezTerm, and popular code editors."

4. **Demo settings version badge (line 346):** Replace hardcoded `v0.2.8` with a data-version span:
   ```html
   <span class="demo-settings-version" data-version>v0.3.9</span>
   ```
   The JS will populate this, but keep v0.3.9 as fallback text.

5. **Terminal carousel (lines 400-431):** Add GNOME Terminal entry. Insert AFTER the Sublime Text entry (line 415) and BEFORE the duplicate section comment (line 416). Use a generic terminal SVG icon:
   ```html
   <span class="terminal-tag"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M2 4a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v16a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V4zm4.5 1.5l4.5 4.5-4.5 4.5-1-1L9 10 5.5 6.5l1-1zM11 15h6v1.5h-6V15z"/></svg> GNOME Terminal</span>
   ```
   ALSO add the same entry in the duplicated aria-hidden set (after the duplicated Sublime Text, line 431):
   ```html
   <span class="terminal-tag" aria-hidden="true"><svg width="14" height="14" viewBox="0 0 24 24" fill="currentColor"><path d="M2 4a2 2 0 0 1 2-2h16a2 2 0 0 1 2 2v16a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V4zm4.5 1.5l4.5 4.5-4.5 4.5-1-1L9 10 5.5 6.5l1-1zM11 15h6v1.5h-6V15z"/></svg> GNOME Terminal</span>
   ```

6. **CTA download button (line 457):** Replace the hardcoded macOS download link:
   ```html
   <a href="#" data-download="macos" target="_blank" rel="noopener" class="btn btn-secondary">
   ```
   This makes the CTA download link use the version-injected URL.

7. **Footer version badge (line 496):** Replace `<span>v0.2.8</span>` with `<span data-version>v0.3.9</span>`.

**home.css additions (append to end of file, before the responsive media queries):**

Add these styles before the `/* ---------- Responsive ---------- */` section (before line 1169):

```css
/* ---------- Platform Badges ---------- */
.platform-badges {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 20px;
  margin-bottom: 40px;
  font-size: 0.82rem;
  color: var(--text-muted);
}

.platform-badge {
  display: inline-flex;
  align-items: center;
  gap: 6px;
}

/* ---------- Download Button Layout ---------- */
.linux-download-wrapper {
  position: relative;
  display: inline-flex;
}

.platform-detected {
  box-shadow: 0 0 0 2px var(--accent);
}

/* ---------- Arch Popup ---------- */
.arch-popup {
  display: none;
  position: absolute;
  top: calc(100% + 8px);
  left: 50%;
  transform: translateX(-50%);
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: 8px;
  min-width: 220px;
  box-shadow: var(--shadow-lg);
  z-index: 10;
}

.arch-popup.open {
  display: block;
}

.arch-option {
  display: flex;
  flex-direction: column;
  padding: 10px 14px;
  border-radius: var(--radius-md);
  color: var(--text-primary);
  text-decoration: none;
  transition: background var(--duration-fast);
}

.arch-option:hover {
  background: var(--bg-code);
}

.arch-option span {
  font-size: 0.8rem;
  color: var(--text-muted);
}
```

Also add in the `@media (max-width: 768px)` block:
```css
.platform-badges {
  gap: 12px;
  font-size: 0.75rem;
}
```
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'); var html=fs.readFileSync('showcase/index.html','utf8'); var css=fs.readFileSync('showcase/css/home.css','utf8'); var htmlChecks=['data-download=\"macos\"','data-download=\"linux_x86\"','data-download=\"linux_arm\"','data-platform=\"linux\"','data-version','GNOME Terminal','platform-badge','arch-popup','linux-download-wrapper','macOS, Windows, and Linux']; var cssChecks=['arch-popup','platform-detected','linux-download-wrapper','platform-badges']; var missing=[]; htmlChecks.forEach(function(s){if(html.indexOf(s)===-1) missing.push('HTML:'+s)}); cssChecks.forEach(function(s){if(css.indexOf(s)===-1) missing.push('CSS:'+s)}); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('All patterns found'); var v028count=(html.match(/v0\\.2\\.8/g)||[]).length; if(v028count>0){console.error('Still has '+v028count+' hardcoded v0.2.8 references');process.exit(1)} console.log('No hardcoded v0.2.8 found')"</automated>
  </verify>
  <done>index.html has three-platform download buttons with data-download attributes, Linux arch popup, Linux platform badge in hero, updated feature card text mentioning Linux, GNOME Terminal in both carousel sets, CTA download uses data-download, demo settings and footer version use data-version. home.css has arch popup, platform-detected, and platform-badges styles. Zero hardcoded v0.2.8 strings remain.</done>
</task>

</tasks>

<verification>
1. Open showcase/index.html in a browser -- verify three download buttons visible in hero
2. Check footer shows v0.3.9
3. Click Linux download button -- verify arch popup appears with x86_64 and aarch64 options
4. Verify GNOME Terminal appears in the terminal carousel
5. Verify feature cards mention "macOS, Windows, and Linux"
6. Verify CTA section download link has data-download attribute
7. Verify demo settings panel shows v0.3.9
</verification>

<success_criteria>
- All 5 previously-hardcoded v0.2.8 strings in index.html are replaced with data-attribute-driven content
- VERSION constant in main.js is single source of truth for all version strings
- Three platform download buttons (macOS, Windows, Linux) visible in hero section
- Linux arch popup functional with x86_64 and aarch64 download links
- OS auto-detection highlights the visitor's platform button
- Feature cards explicitly mention Linux
- GNOME Terminal in carousel (both original and aria-hidden duplicate sets)
- Linux platform badge visible in hero
</success_criteria>

<output>
After completion, create `.planning/phases/36-showcase-website-update/36-01-SUMMARY.md`
</output>
