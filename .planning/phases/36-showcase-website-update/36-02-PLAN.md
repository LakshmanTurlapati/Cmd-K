---
phase: 36-showcase-website-update
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - showcase/privacy.html
autonomous: true
requirements:
  - WEB-04-PRIVACY

must_haves:
  truths:
    - "Privacy policy shows 'Last updated: March 15, 2026'"
    - "Permissions section covers macOS, Windows, and Linux with platform-specific details"
    - "Linux-specific data handling is documented (/proc, xdotool, AT-SPI2)"
    - "Credential store mentions Linux system keyring via libsecret"
    - "March 9, 2026 policy is preserved verbatim in a collapsible section"
    - "Terminal Mode data list includes recent terminal text with budget mention"
    - "Footer version badge uses data-version attribute for JS population"
  artifacts:
    - path: "showcase/privacy.html"
      provides: "Updated privacy policy with Linux details and version history"
      contains: "data-version"
  key_links:
    - from: "showcase/js/main.js"
      to: "showcase/privacy.html"
      via: "data-version attribute on footer badge"
      pattern: "data-version"
---

<objective>
Update the privacy policy page with v0.3.9 Linux-specific data handling details, preserve the current March 9 policy as a collapsible version history entry, and update metadata.

Purpose: Users on Linux need to understand what system APIs CMD+K accesses (/proc, xdotool, AT-SPI2). The privacy policy must be accurate for all three platforms.
Output: Updated privacy.html with Linux coverage and version history.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/36-showcase-website-update/36-CONTEXT.md
@.planning/phases/36-showcase-website-update/36-RESEARCH.md
@showcase/privacy.html
@showcase/css/privacy.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update privacy policy content for v0.3.9 with Linux details</name>
  <files>showcase/privacy.html</files>
  <action>
Make the following changes to privacy.html. IMPORTANT: Before editing the main policy sections, copy the ENTIRE current policy content (from the TLDR callout through the Contact section, lines 64-172) to use verbatim in the March 9 version history entry.

**1. Last updated date (line 59):**
Change `Last updated: March 9, 2026` to `Last updated: March 15, 2026`

**2. Terminal Mode data list (lines 78-86):**
Add a new bullet after "Last 25 lines of visible terminal output":
```html
<li>Recent terminal text (visible buffer content, up to approximately 12% of the model's context window)</li>
```

**3. Permissions section (line 156):**
Replace the single paragraph with platform-specific details:
```html
<h2>Permissions</h2>
<p>CMD+K uses platform APIs to read terminal output and detect the active application:</p>
<ul>
  <li><strong>macOS</strong> &mdash; Accessibility API permission, granted in System Settings &gt; Privacy &amp; Security &gt; Accessibility. You can revoke it at any time, which will prevent CMD+K from capturing terminal context.</li>
  <li><strong>Windows</strong> &mdash; No special permissions needed. CMD+K uses standard Win32 APIs (UI Automation, CreateToolhelp32Snapshot) that work without elevation.</li>
  <li><strong>Linux</strong> &mdash; <code>/proc</code> filesystem for process detection (standard, no special permissions). <code>xdotool</code> for keystroke simulation on X11. AT-SPI2 accessibility API for reading terminal text from VTE-based terminals (GNOME Terminal, Tilix, Terminator). <code>kitty</code> and <code>WezTerm</code> remote-control APIs for those terminals.</li>
</ul>
```

**4. Where Your Data Is Stored -- credential store (line 138):**
Update the system credential store bullet to include Linux:
```html
<li><strong>System credential store</strong> Your API key(s), encrypted by the operating system (macOS Keychain on Mac, Windows Credential Manager on Windows, system keyring via libsecret on Linux)</li>
```

**5. What Is NOT Sent -- API key line (line 124):**
Update to include all three platforms:
```html
<li>Your API key (stored in macOS Keychain, Windows Credential Manager, or Linux system keyring &mdash; never included in prompts)</li>
```

**6. Policy History section (lines 174-186):**
Add a new `<details>` entry for the March 9, 2026 version ABOVE the existing February 27 entry. The March 9 entry must contain the EXACT full text of the current policy (before the v0.3.9 edits). This means you need to:
- Copy lines 64-172 verbatim as they currently exist (the March 9 version text)
- Wrap in a new `<details class="policy-version">` block
- Summary text: `<span class="policy-version-date">March 9, 2026</span> Multi-provider support &mdash; macOS and Windows`

The structure should be:
```html
<h2>Policy History</h2>
<details class="policy-version">
  <summary>
    <span class="policy-version-date">March 9, 2026</span>
    Multi-provider support &mdash; macOS and Windows
  </summary>
  <div class="policy-version-body">
    <!-- EXACT verbatim copy of the current March 9 policy text (TLDR through Contact) -->
  </div>
</details>
<details class="policy-version">
  <summary>
    <span class="policy-version-date">February 27, 2026</span>
    Initial version &mdash; xAI-only provider support
  </summary>
  <div class="policy-version-body">
    <!-- Existing Feb 27 entry content, unchanged -->
  </div>
</details>
```

**7. Footer version badge (line 221):**
Replace `<span>v0.2.8</span>` with `<span data-version>v0.3.9</span>`.
Keep v0.3.9 as fallback text; the JS from main.js (loaded on this page too) will populate via data-version attribute.

**CRITICAL:** The March 9 version history entry must preserve the EXACT original text, NOT a summary. Copy the full policy sections verbatim before making any edits.
  </action>
  <verify>
    <automated>node -e "var fs=require('fs'); var c=fs.readFileSync('showcase/privacy.html','utf8'); var checks=['March 15, 2026','libsecret','xdotool','AT-SPI2','/proc','data-version','March 9, 2026','February 27, 2026','Multi-provider support','Windows Credential Manager','12%','linux-download-wrapper'.replace('linux-download-wrapper','Linux')]; checks=['March 15, 2026','libsecret','xdotool','AT-SPI2','/proc','data-version','March 9, 2026','February 27, 2026','Multi-provider support','Win32 APIs']; var missing=checks.filter(function(s){return c.indexOf(s)===-1}); if(missing.length){console.error('Missing:',missing);process.exit(1)} console.log('All patterns found'); var details=(c.match(/<details class=\"policy-version\">/g)||[]).length; if(details<2){console.error('Expected at least 2 policy-version details, found '+details);process.exit(1)} console.log(details+' policy history entries found'); var v028=(c.match(/v0\\.2\\.8/g)||[]).length; if(v028>0){console.error('Still has '+v028+' hardcoded v0.2.8');process.exit(1)} console.log('No hardcoded v0.2.8')"</automated>
  </verify>
  <done>Privacy policy updated with: March 15 2026 date, Linux-specific permissions (proc, xdotool, AT-SPI2), Linux credential store (libsecret), terminal text budget mention, March 9 policy preserved verbatim in collapsible history, footer version using data-version attribute, zero hardcoded v0.2.8 strings.</done>
</task>

</tasks>

<verification>
1. Open showcase/privacy.html in a browser
2. Verify "Last updated: March 15, 2026" at top
3. Verify Permissions section has macOS, Windows, and Linux details
4. Verify credential store mentions libsecret
5. Expand "March 9, 2026" collapsible -- verify full original policy text preserved
6. Expand "February 27, 2026" collapsible -- verify unchanged
7. Verify footer shows v0.3.9
</verification>

<success_criteria>
- Privacy policy date is March 15, 2026
- Linux-specific APIs documented: /proc, xdotool, AT-SPI2, kitty remote control, WezTerm CLI
- Credential storage includes all three platform stores
- March 9 policy preserved as full verbatim text in collapsible section
- Two policy history entries (March 9 and February 27)
- Footer version badge uses data-version attribute
- No hardcoded v0.2.8 strings remain
</success_criteria>

<output>
After completion, create `.planning/phases/36-showcase-website-update/36-02-SUMMARY.md`
</output>
