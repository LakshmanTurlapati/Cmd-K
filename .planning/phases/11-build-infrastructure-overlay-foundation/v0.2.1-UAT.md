---
status: testing
phase: v0.2.1-windows-support
source: [11-VERIFICATION.md, 12-VERIFICATION.md, 13-VERIFICATION.md, 14-VERIFICATION.md, 15-VERIFICATION.md, 16-VERIFICATION.md]
started: 2026-03-03T12:30:00Z
updated: 2026-03-03T12:30:00Z
---

## Current Test

number: 1
name: Overlay Appears with Vibrancy
expected: |
  Press Ctrl+Shift+K on Windows. The CMD+K overlay appears with a dark frosted glass (Acrylic blur) background, floating above all windows.
awaiting: user response

## Tests

### 1. Overlay Appears with Vibrancy
expected: Press Ctrl+Shift+K on Windows. The CMD+K overlay appears with a dark frosted glass (Acrylic blur) background, floating above all windows.
result: [pending]

### 2. Alt+Tab and Taskbar Exclusion
expected: With the overlay open, press Alt+Tab. The CMD+K overlay does NOT appear in the Alt+Tab switcher or the Windows taskbar.
result: [pending]

### 3. Focus Restore on Escape
expected: Press Ctrl+Shift+K while a terminal window is focused. Then press Escape. Focus returns automatically to the terminal -- no clicking needed.
result: [pending]

### 4. Terminal Context -- Windows Terminal
expected: Open Windows Terminal, press Ctrl+Shift+K. The overlay detects the shell type (e.g. powershell) and shows the current working directory.
result: [pending]

### 5. Terminal Context -- PowerShell
expected: Open standalone PowerShell (not inside Windows Terminal), press Ctrl+Shift+K. Shell type and CWD are detected.
result: [pending]

### 6. Terminal Context -- CMD
expected: Open CMD, press Ctrl+Shift+K. Shell type (cmd) and CWD are detected.
result: [pending]

### 7. Elevated Process Handling
expected: Open PowerShell as Administrator, press Ctrl+Shift+K. CWD may show as unavailable (graceful fallback). No crash or error.
result: [pending]

### 8. AI Generates Windows Commands
expected: Type a request like "list all files in current directory". AI generates a Windows-appropriate command (e.g. Get-ChildItem or dir, not ls).
result: [pending]

### 9. Destructive Command Safety
expected: Type something like "delete everything in C drive". The generated command triggers a destructive command safety warning before any action.
result: [pending]

### 10. Paste into Terminal
expected: Accept a generated command. The command is pasted into the active terminal via clipboard + Ctrl+V. Terminal receives the text.
result: [pending]

### 11. Confirm Execution
expected: After paste, press Enter confirmation in the overlay. The Enter keystroke is sent to the terminal and the command executes.
result: [pending]

### 12. Elevated Paste Warning
expected: Try the paste flow with an elevated (admin) terminal. A warning is shown about the elevated terminal instead of silent failure.
result: [pending]

### 13. Onboarding Skips Accessibility
expected: On first launch (or after clearing settings), the onboarding wizard does NOT show a macOS accessibility permission step. Goes straight from API key to hotkey setup.
result: [pending]

### 14. Keyboard Labels Show Ctrl
expected: In settings or onboarding, keyboard shortcuts display "Ctrl" (not "Cmd") and "Alt" (not "Option").
result: [pending]

### 15. System Tray Menu
expected: Right-click the CMD+K system tray icon. A context menu appears with options (Settings, Quit, etc.).
result: [pending]

### 16. UIA Terminal Output Reading
expected: Run a few commands in Windows Terminal so there's visible output. Press Ctrl+Shift+K and type a follow-up question about the output (e.g. "explain the last error"). The AI has context about what's on screen.
result: [pending]

### 17. NSIS Installer
expected: Running cargo tauri build on Windows produces an NSIS .exe installer in target/release/bundle/nsis/. The installer runs per-user without requiring admin.
result: [pending]

## Summary

total: 17
passed: 0
issues: 0
pending: 17
skipped: 0

## Gaps

[none yet]
