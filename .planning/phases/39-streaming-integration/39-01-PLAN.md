---
phase: 39-streaming-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Settings/ModelTab.tsx
autonomous: true
requirements:
  - LSTR-01
  - LSTR-02
  - LSTR-03

must_haves:
  truths:
    - "Local provider cost display shows $0.00 with token counts instead of $---"
    - "Token usage counts appear in settings after local provider queries"
    - "Streaming from local providers works end-to-end (already implemented in Phase 37)"
    - "120s timeout handles cold-start model loading (already implemented in Phase 37)"
  artifacts:
    - path: "src/components/Settings/ModelTab.tsx"
      provides: "Cost display fix for local providers"
      contains: "$0.00"
  key_links:
    - from: "src/components/Settings/ModelTab.tsx"
      to: "src/store/index.ts"
      via: "PROVIDERS array local flag lookup"
      pattern: "PROVIDERS.*local"
---

<objective>
Fix local provider cost display and verify end-to-end streaming integration.

Purpose: Phase 37 already implemented the full streaming pipeline (dynamic URL, auth bypass, 120s timeout, token tracking). This plan fixes the one remaining UI issue -- local providers showing "$---" instead of "$0.00" for cost -- and verifies everything works together.

Output: Updated ModelTab.tsx with correct local provider cost display.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-streaming-integration/39-CONTEXT.md

@src/components/Settings/ModelTab.tsx
@src/store/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix local provider cost display to show $0.00</name>
  <files>src/components/Settings/ModelTab.tsx</files>
  <read_first>
    - src/components/Settings/ModelTab.tsx (full file, especially lines 207-262 cost display section)
    - src/store/index.ts (PROVIDERS array with local flag)
  </read_first>
  <action>
In the "Estimated Cost" section of ModelTab.tsx (around line 207-262), modify the cost display logic to distinguish local providers from cloud providers when `pricing_available` is false.

Currently, when `allUnpriced` is true (line 240), it shows: `{tokenStr} — $—`

Change the logic so that when ALL unpriced entries are from local providers, it shows `$0.00` with the token string instead of `$---`. Use the PROVIDERS array (already imported) to determine if a provider is local by matching `entry.provider` against `PROVIDERS.find(p => p.name === entry.provider)?.local`.

Specifically:

1. After the `allUnpriced` and `someUnpriced` const declarations (lines 217-218), add a check to determine if all unpriced entries are from local providers:
   ```typescript
   const allUnpricedAreLocal = allUnpriced && usageStats.entries.every((e) => {
     const prov = PROVIDERS.find((p) => p.name === e.provider);
     return prov?.local ?? false;
   });
   ```

2. In the JSX (line 240), change the `allUnpriced` branch:
   - If `allUnpricedAreLocal` is true: show `$0.00 — {tokenStr}` (same format as cloud providers but with $0.00 cost, add title="Free (local model)")
   - If `allUnpriced` but NOT all local: keep the existing `$---` display

3. For the `someUnpriced` case: if the unpriced entries are all local, treat them as $0.00 cost entries (they are free) rather than showing the asterisk disclaimer. Check: `const unpricedAreLocal = usageStats.entries.filter(e => !e.pricing_available).every(e => PROVIDERS.find(p => p.name === e.provider)?.local ?? false);` — if true, suppress the asterisk and footnote.

Keep the sparkline behavior unchanged -- local queries will have `null` cost entries which is fine (sparkline only renders when `hasCosts` is true).
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/CMD+K/Cmd-K && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "allUnpricedAreLocal" src/components/Settings/ModelTab.tsx
    - grep -q '\\$0\\.00' src/components/Settings/ModelTab.tsx
    - grep -q "Free (local" src/components/Settings/ModelTab.tsx
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  <done>
    - When only local provider usage exists, cost display shows "$0.00" with token counts instead of "$---"
    - When mixed local+cloud usage exists with cloud unpriced, asterisk still shows for cloud-unpriced only
    - TypeScript compiles without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify streaming pipeline compilation and existing tests</name>
  <files></files>
  <read_first>
    - src-tauri/src/commands/ai.rs (lines 207-224, local provider URL resolution)
    - src-tauri/src/commands/providers/openai_compat.rs (stream function signature)
    - src-tauri/src/commands/providers/mod.rs (default_timeout_secs, is_local)
  </read_first>
  <action>
Run the full verification suite to confirm Phase 37's streaming plumbing compiles and all existing tests pass. This covers LSTR-01 (streaming via OpenAI-compat adapter) and LSTR-02 (120s timeout) which were already implemented.

1. Run `cargo test --lib` to verify all Rust tests pass (including is_local, default_timeout_secs tests)
2. Run `npx tsc --noEmit` to verify TypeScript compiles after Task 1 changes
3. Verify key code exists by reading:
   - ai.rs: confirm `provider.is_local()` branch resolves URL and sets empty api_key
   - openai_compat.rs: confirm `api_url` parameter and conditional auth header
   - mod.rs: confirm `default_timeout_secs()` returns 120 for Ollama/LMStudio

No code changes needed -- this is a verification-only task confirming LSTR-01 and LSTR-02 are already satisfied by Phase 37.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/CMD+K/Cmd-K && cargo test --lib 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - cargo test --lib exits 0 with all tests passing
    - npx tsc --noEmit exits 0
    - grep -q "is_local" src-tauri/src/commands/ai.rs (confirms local URL resolution exists)
    - grep -q "default_timeout_secs" src-tauri/src/commands/providers/mod.rs (confirms timeout function exists)
  </acceptance_criteria>
  <done>
    - All Rust tests pass confirming streaming pipeline compiles correctly
    - TypeScript compiles confirming frontend changes are valid
    - LSTR-01 (streaming via OpenAI-compat) verified as implemented
    - LSTR-02 (120s timeout) verified as implemented
  </done>
</task>

</tasks>

<verification>
1. `cargo test --lib` -- all Rust tests pass
2. `npx tsc --noEmit` -- TypeScript compiles clean
3. Manual: Select Ollama, run a query, check Settings > Estimated Cost shows "$0.00" with token counts
</verification>

<success_criteria>
- Local provider cost display shows "$0.00 — X in / Y out" instead of "$--- — X in / Y out"
- All existing tests pass (cargo test --lib)
- TypeScript compiles without errors
- Streaming pipeline code confirmed present (is_local URL resolution, 120s timeout, token tracking)
</success_criteria>

<output>
After completion, create `.planning/phases/39-streaming-integration/39-01-SUMMARY.md`
</output>
