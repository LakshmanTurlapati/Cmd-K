---
phase: 28-uia-terminal-text-scoping
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/terminal/mod.rs
autonomous: true
requirements:
  - UIAS-02

must_haves:
  truths:
    - "Single Linux path in text does NOT trigger WSL detection"
    - "Linux path plus prompt pattern DOES trigger WSL detection"
    - "WSL mount path (/mnt/c/) alone triggers WSL detection (strong signal)"
    - "Editor-like content (Dockerfiles, READMEs with Linux paths) does not false-positive"
  artifacts:
    - path: "src-tauri/src/terminal/mod.rs"
      provides: "Multi-signal detect_wsl_from_text with scoring threshold"
      contains: "score >= 2"
  key_links:
    - from: "src-tauri/src/terminal/mod.rs::detect_wsl_from_text"
      to: "src-tauri/src/terminal/mod.rs::detect_full_with_hwnd"
      via: "called at line 170 for WSL text detection"
      pattern: "detect_wsl_from_text"
---

<objective>
Harden `detect_wsl_from_text` to require multiple corroborating signals before declaring WSL, eliminating false positives from editor content containing Linux paths.

Purpose: A single Linux path like `/home/user` in an open editor file currently triggers WSL mode even when the terminal is running PowerShell on Windows. This breaks command generation.
Output: Refactored `detect_wsl_from_text` with scoring system (threshold >= 2) and comprehensive unit tests.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-uia-terminal-text-scoping/28-RESEARCH.md

<interfaces>
<!-- Current function signature and call site in mod.rs -->

From src-tauri/src/terminal/mod.rs (line 609):
```rust
#[cfg(target_os = "windows")]
fn detect_wsl_from_text(text: &str) -> bool {
    // Currently returns true on ANY single Linux path match
    // or ANY single prompt pattern match
}
```

Called at line 170:
```rust
if !terminal.is_wsl {
    if detect_wsl_from_text(text) {
        eprintln!("[detect_full_with_hwnd] WSL detected from UIA text (process tree missed it)");
        terminal.is_wsl = true;
    }
}
```
</interfaces>
</context>

<feature>
  <name>Multi-Signal WSL Text Detection</name>
  <files>src-tauri/src/terminal/mod.rs</files>
  <behavior>
    Test cases for detect_wsl_from_text (all run on non-Windows too via cfg removal for test module):

    MUST RETURN FALSE (single weak signal):
    - Text containing only "/home/user/.bashrc" with no prompt -> false
    - Text containing only "/etc/nginx/nginx.conf" (editor content) -> false
    - Text containing "FROM ubuntu:22.04\nRUN apt-get update\nCOPY /home/app/config ." (Dockerfile) -> false
    - Text containing "PS C:\Users\dev> cat /home/user/readme.md" (PowerShell viewing Linux path) -> false
    - Text with just "user@host" but no path or prompt ending -> false

    MUST RETURN TRUE (multiple signals or strong signal):
    - Text with "/mnt/c/Users/dev" (WSL mount = score 2) -> true
    - Text with "/mnt/d/projects" (WSL mount = score 2) -> true
    - Text with "user@ubuntu:/home/user$" (path + prompt pattern = score 2) -> true
    - Text with "/home/user\nuser@host:~$" (Linux path + prompt = score 2) -> true
    - Text with "user@myhost:~/projects$ ls\nfile1 file2\nuser@myhost:~/projects$" (prompt pattern + ending = score 2) -> true

    MUST PRESERVE existing behavior for clear WSL terminals:
    - Full WSL terminal output with prompt and Linux paths -> true
    - "/mnt/c/" alone without any other signal -> true (strong signal, score 2)
  </behavior>
  <implementation>
    Replace the current detect_wsl_from_text with a scoring system:

    1. Remove the #[cfg(target_os = "windows")] from detect_wsl_from_text so tests can run on Linux (WSL2 build host). Add #[cfg(target_os = "windows")] only on the call site, or use cfg_attr. Alternatively, keep the cfg gate but add a parallel test-only version. Simplest: remove cfg from the function itself (it only does string matching, no Windows APIs), add #[allow(dead_code)] for non-Windows.

    2. Implement scoring:
       - score += 1 for Linux paths (/home/, /root/, /var/, /etc/, /usr/, /tmp/, /opt/)
       - score += 2 for WSL mount paths (/mnt/c/, /mnt/d/, etc.)
       - score += 1 for user@host:/path or user@host:~ prompt pattern
       - score += 1 for user@host...$ or user@host...# prompt ending (without backslash)
       - Return score >= 2

    3. Keep all eprintln! diagnostics, updated to show score.

    4. Add #[cfg(test)] mod tests at the bottom of mod.rs (or extend existing if present) with all behavior cases above.

    Important: The function signature (fn detect_wsl_from_text(text: &str) -> bool) MUST NOT change -- it's called from detect_full_with_hwnd and the return type is used directly in an if condition.
  </implementation>
</feature>

<verification>
cargo test --lib -p cmd-k -- detect_wsl --nocapture
All tests pass. No regressions in existing tests: cargo test --lib -p cmd-k
</verification>

<success_criteria>
- detect_wsl_from_text uses scoring with threshold >= 2
- Single Linux path alone returns false (the key fix)
- WSL mount path alone returns true (strong signal)
- Prompt + path combination returns true
- All existing tests still pass (cargo test --lib -p cmd-k)
- New unit tests cover all behavior cases listed above
</success_criteria>

<output>
After completion, create `.planning/phases/28-uia-terminal-text-scoping/28-01-SUMMARY.md`
</output>
