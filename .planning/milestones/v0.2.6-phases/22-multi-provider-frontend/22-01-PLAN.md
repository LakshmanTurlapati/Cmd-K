---
phase: 22-multi-provider-frontend
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/store/index.ts
  - src/components/Onboarding/StepProviderSelect.tsx
  - src/components/Onboarding/OnboardingWizard.tsx
  - src/components/Onboarding/StepApiKey.tsx
  - src/components/Onboarding/StepDone.tsx
  - src/App.tsx
autonomous: true
requirements: [PFUI-01, PFUI-05]

must_haves:
  truths:
    - "New user sees provider selection as the first onboarding step with 5 providers listed"
    - "No provider is pre-selected -- user must explicitly choose one"
    - "After selecting a provider, onboarding proceeds to API Key step which adapts text to that provider"
    - "Onboarding stepper shows 5 nodes instead of 4"
    - "selectedProvider is loaded from settings.json on app startup before API key validation"
    - "Switching providers does not affect conversation history (turnHistory tied to windowKey)"
  artifacts:
    - path: "src/components/Onboarding/StepProviderSelect.tsx"
      provides: "Provider selection step component"
      min_lines: 30
    - path: "src/store/index.ts"
      provides: "selectedModels map and provider constants"
      contains: "selectedModels"
    - path: "src/components/Onboarding/OnboardingWizard.tsx"
      provides: "5-step wizard with provider step first"
      contains: "TOTAL_STEPS = 5"
  key_links:
    - from: "src/components/Onboarding/StepProviderSelect.tsx"
      to: "src/store/index.ts"
      via: "setSelectedProvider action"
      pattern: "setSelectedProvider"
    - from: "src/App.tsx"
      to: "settings.json"
      via: "Store.load on startup"
      pattern: "store\\.get.*selectedProvider"
    - from: "src/components/Onboarding/OnboardingWizard.tsx"
      to: "src/components/Onboarding/StepProviderSelect.tsx"
      via: "step 0 renders StepProviderSelect"
      pattern: "StepProviderSelect"
---

<objective>
Create the provider selection onboarding step and store foundation for multi-provider support.

Purpose: Users must be able to choose their AI provider during first-run onboarding, and the app must remember this choice across restarts. This plan builds the store layer (per-provider model memory, provider persistence) and the onboarding flow (new provider step, 5-step wizard, provider-aware text).

Output: StepProviderSelect component, 5-step OnboardingWizard, provider-aware StepApiKey/StepDone text, store with selectedModels map, App.tsx loading selectedProvider on startup.
</objective>

<execution_context>
@/home/parzival/.claude/get-shit-done/workflows/execute-plan.md
@/home/parzival/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-multi-provider-frontend/22-CONTEXT.md
@.planning/phases/22-multi-provider-frontend/22-RESEARCH.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From src/store/index.ts:
```typescript
export interface ModelWithMeta {
  id: string;
  label: string;
  tier: string;  // "fast" | "balanced" | "capable" | "" (uncategorized)
}

// Existing store state (relevant subset):
selectedProvider: string;  // default: "xai"
selectedModel: string | null;
availableModels: ModelWithMeta[];
apiKeyStatus: "unknown" | "validating" | "valid" | "invalid" | "error";
apiKeyLast4: string;
onboardingStep: number;
onboardingComplete: boolean;

// Existing actions:
setSelectedProvider: (provider: string) => void;
setModels: (models: ModelWithMeta[]) => void;
setSelectedModel: (model: string) => void;
setApiKeyStatus: (status: ...) => void;
setApiKeyLast4: (last4: string) => void;
```

From src/components/Onboarding/OnboardingWizard.tsx:
```typescript
// Currently 4 steps: StepApiKey (0), StepModelSelect (1), StepAccessibility (2), StepDone (3)
const TOTAL_STEPS = 4;
const stepLabels = ["API Key", "Model", "Accessibility", "Done"];
// Windows skips Accessibility step (index 2)
```

Backend provider IDs (must match Rust enum serde names):
"openai", "anthropic", "gemini", "xai", "openrouter"

Backend IPC commands (all accept `provider` param already):
- invoke("validate_api_key", { provider, apiKey })
- invoke("fetch_models", { provider, apiKey })
- invoke("get_api_key", { provider })
- invoke("save_api_key", { provider, key })
- invoke("delete_api_key", { provider })
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add store foundation and provider constants</name>
  <files>src/store/index.ts</files>
  <action>
Add a PROVIDERS constant array at the top of the store file (exported, used by all UI components):

```typescript
export const PROVIDERS = [
  { id: "openai", name: "OpenAI" },
  { id: "anthropic", name: "Anthropic" },
  { id: "gemini", name: "Google Gemini" },
  { id: "xai", name: "xAI" },
  { id: "openrouter", name: "OpenRouter" },
] as const;
```

Add to OverlayState interface:
- `selectedModels: Record<string, string>` -- per-provider model memory map (e.g., `{ "openai": "gpt-4o", "xai": "grok-3" }`)
- `setSelectedModels: (models: Record<string, string>) => void` -- setter for the map

Add to store initial state:
- `selectedModels: {}` (empty object)

Add the setter implementation:
- `setSelectedModels: (models) => set({ selectedModels: models })`

Keep all existing state and actions unchanged. The `selectedModel` remains the "currently active" model. `selectedModels` is the persistence layer for per-provider memory.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/CMD+K/Cmd-K && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PROVIDERS constant exported, selectedModels map added to store with setter, TypeScript compiles</done>
</task>

<task type="auto">
  <name>Task 2: Create StepProviderSelect and wire 5-step onboarding</name>
  <files>src/components/Onboarding/StepProviderSelect.tsx, src/components/Onboarding/OnboardingWizard.tsx, src/components/Onboarding/StepApiKey.tsx, src/components/Onboarding/StepDone.tsx, src/App.tsx</files>
  <action>
**StepProviderSelect.tsx (NEW):**
Create provider selection component following existing step pattern (takes `onNext` prop). Per user decisions:
- Vertical icon + name list with 5 providers from PROVIDERS constant (import from store)
- No default pre-selected -- user must explicitly click one. Use local `chosen` state, initialize to null.
- Provider icons: styled text initials in circles matching glassmorphic aesthetic. "O" for OpenAI, "A" for Anthropic, "G" for Gemini, "x" for xAI, "OR" for OpenRouter. Use `w-8 h-8 rounded-full bg-white/10 flex items-center justify-center text-xs font-medium text-white/70` for the initial circle.
- On select: call `setSelectedProvider(providerId)` and set local `chosen` state.
- Also persist provider to settings.json: `Store.load("settings.json")` then `store.set("selectedProvider", providerId)` then `store.save()`.
- "Next" button disabled until a provider is chosen (same styling pattern as StepApiKey's disabled Next button).
- Selected provider button gets `bg-white/15 text-white border border-white/20` styling; unselected gets `bg-white/5 hover:bg-white/8 text-white/70 border border-transparent`.
- Each button is a flex row: `[initial circle] [provider name]` with `gap-3 items-center`.

**OnboardingWizard.tsx:**
- Change `TOTAL_STEPS = 4` to `TOTAL_STEPS = 5`
- Update `stepLabels` to `["Provider", "API Key", "Model", "Accessibility", "Done"]`
- Import `StepProviderSelect` from `./StepProviderSelect`
- Update step rendering: step 0 = StepProviderSelect, step 1 = StepApiKey, step 2 = StepModelSelect, step 3 = StepAccessibility, step 4 = StepDone
- Update Windows skip logic: skip Accessibility step at new index 3 (was 2). Change `if (isWindows() && nextStep === 2)` to `if (isWindows() && nextStep === 3)` and skip to `nextStep = 4`.
- Update "Skip this step" condition from `onboardingStep < 3` to `onboardingStep < 4`

**StepApiKey.tsx:**
- Replace hardcoded "Paste your xAI API key" placeholder with provider-aware text: import `PROVIDERS` from store, then `PROVIDERS.find(p => p.id === selectedProvider)?.name ?? selectedProvider`. Placeholder becomes `Paste your ${providerName} API key`.
- Replace "Enter your xAI API key to enable AI-powered commands." with `Enter your ${providerName} API key to enable AI-powered commands.`

**StepDone.tsx:**
- Add a "Provider" row in the configuration summary, above the Hotkey row. Display the selected provider name using PROVIDERS lookup. Read `selectedProvider` from store.
- Format: same as existing rows -- left label "PROVIDER" (uppercase tracking-wider), right value = provider name.

**App.tsx:**
- In `checkOnboarding`, BEFORE the API key check, load `selectedProvider` from settings.json: `const savedProvider = await store.get<string>("selectedProvider");` If truthy, call `useOverlayStore.getState().setSelectedProvider(savedProvider);`
- Also load `selectedModels` map: `const savedModels = await store.get<Record<string, string>>("selectedModels");` If truthy, call `useOverlayStore.getState().setSelectedModels(savedModels);`
- Then load `selectedModel` for the active provider from the map: `if (savedModels && savedProvider && savedModels[savedProvider]) { setSelectedModel(savedModels[savedProvider]); }` -- this replaces the existing single `selectedModel` load for the onboarding-complete path.
- Handle v0.2.4 upgrade: if `onboardingComplete` is true but `selectedProvider` is not saved, the user was on xAI. The default "xai" in the store already handles this, so no explicit migration is needed.
- Handle in-progress onboarding upgrade: if `onboardingComplete` is false and `onboardingStep` was saved from v0.2.4 (old 4-step), the step indices are different. Since the new step 0 is Provider and old step 0 was API Key, reset to step 0 if no `selectedProvider` is saved (forces user to pick provider first). If `selectedProvider` IS saved, increment old step by 1 to account for the new Provider step at index 0.

**Race condition guard (Pitfall 2 from research):**
In StepApiKey.tsx, add a providerRef (useRef) that tracks the current provider. In the checkStoredKey effect and debounced validation, check `if (providerRef.current !== selectedProvider) return;` before setting state from async results. Update providerRef.current at the start of each effect.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/laksh/OneDrive/Documents/Codes/CMD+K/Cmd-K && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>StepProviderSelect component created with 5 provider options (no default selected), OnboardingWizard updated to 5 steps with Provider first, StepApiKey shows provider-aware text with race condition guard, StepDone shows provider in summary, App.tsx loads selectedProvider and selectedModels from settings.json before API key check, TypeScript compiles cleanly</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with no errors
- StepProviderSelect.tsx exists with PROVIDERS list rendering
- OnboardingWizard.tsx has TOTAL_STEPS = 5 and renders StepProviderSelect at step 0
- StepApiKey.tsx has no hardcoded "xAI" text
- App.tsx loads selectedProvider from settings.json before API key validation
- store/index.ts exports PROVIDERS constant and has selectedModels in state
</verification>

<success_criteria>
- TypeScript compiles cleanly
- 5-step onboarding flow with Provider selection as step 0
- Provider-aware placeholder text in API key input
- selectedProvider persisted to and loaded from settings.json
- selectedModels map in store for per-provider model memory
- Race condition guard in StepApiKey prevents stale async results
</success_criteria>

<output>
After completion, create `.planning/phases/22-multi-provider-frontend/22-01-SUMMARY.md`
</output>
